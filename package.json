{
  "name": "paradis-immo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "start": "pnpm -r start",
    "test": "pnpm --filter api test",
    "test:e2e": "pnpm --filter api test:e2e",
    "lint": "pnpm -r lint",
    "export:openapi": "pnpm --filter api export:openapi",
    "generate:types": "node scripts/generate-types.mjs"
  },
  "devDependencies": {
    "openapi-typescript": "^7.4.4"
  },
  "engines": {
    "node": ">=18"
  }
}